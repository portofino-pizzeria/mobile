{
  "schema_version": 1,
  "base_sha": "35bf7e0de268e485b81aef5a3fa1068c385a6754",
  "ours_sha": "077b3005299cc62c4ae18591acd5e8f07bd95b90",
  "theirs_sha": "e58e195416b27ec654b68d21fe90a310145e5825",
  "entries": [
    {
      "path": "plans/2026-09-10-automatic-deployment.md",
      "base_oid": "59410e81943aedf19b269065f97e09a294e9e8a2",
      "ours_oid": "b28d33d7bf8d30fc2e04b6d5a8f4f6e45cf9c6d7",
      "theirs_oid": "a97f8d95c67655212c0133e1c6e2efea91ec5b10",
      "base_path": "plans/2026-09-10-automatic-deployment.md.base",
      "ours_path": "plans/2026-09-10-automatic-deployment.md.ours",
      "theirs_path": "plans/2026-09-10-automatic-deployment.md.theirs",
      "hunks": [
        "<<<<<<< HEAD\n> **Status:** IMPLEMENTED 2026-09-12 — every phase is merged; NOT yet SHIPPED,\n> because the plan's own \"Verification of this plan's own work\" is only partly\n> met. Ledger, measured 2026-09-12 rather than inferred from merges:\n>\n> | Phase | Landed as | State |\n> |---|---|---|\n> | 1 web marker | `mobile#6` | live — `<meta name=\"build-sha\">` on portofino-essen.com reads `35bf7e0`, the tip of `main` |\n> | 1 backend marker | `backend#1` | merged; **not live** — `api.portofino-essen.com/api/health` still answers without `commit` (a 2026-09-03 image) |\n> | 2 OIDC + roles + SSM | `infra#1`, `3e02a93` | **applied** — both roles exist, all three `/portofino/production/*` parameters exist |\n> | 3 web deploy | `mobile#6`, `ebd9dc6` | working — push-triggered runs green, marker moves (verification 1 ✔) |\n> | 4a backend CI | `backend#1` | working — green on every PR and master push |\n> | 4b backend deploy | `backend#1`, `#2` | **has never reached AWS** — the three master pushes so far all stopped at the reviewer gate, which was unconfigurable on the Free plan at the time; the environment now carries a required reviewer and the four repo variables are set, so the NEXT master push is the first real run |\n> | 5 retire the Terraform path | `infra#1` | merged; apply-then-plan (verification 5) not recorded |\n>\n> Outstanding before SHIPPED: verification 2 (a backend commit moves\n> `/api/health` `commit` — the merge of `backend#4`, which adds the retag\n> rollback path, is the natural first candidate; it needs the reviewer to\n> approve at the gate), 3 (a web deploy observed mid-flight from a warm-cache\n> browser), 4 (both rollbacks executed — the one `workflow_dispatch` of Deploy\n> web on 2026-09-11 deployed the tip of `main`, not an older sha, so neither\n> rollback has been exercised), and 5.\n>\n> Superseded by implementation: the backend rollback is retag-first rather\n> than rebuild-only (`ecr:BatchGetImage` was granted by `infra@425abd3` for\n> exactly this; `backend#4` retags when a `:<sha>` image exists and rebuilds\n> only when none does, or on an explicit `rebuild: true` dispatch). The backend OIDC subject pins `environment:production-backend`\n> rather than the ref — see `infra/github-oidc.tf`'s comment for why the\n> ref form could never match.\n>\n> Previously: VETTED 2026-09-10.\n=======\n> **Status: SHIPPED 2026-09-12 — every phase has landed in its repo; the web\n> path is proved live, the backend path is one operator approval from it.**\n> Per phase, with the evidence read on 2026-09-12 (not the PRs' own claims):\n>\n> | Phase | Repo | Landed as | Live? |\n> |---|---|---|---|\n> | 1 web — `build-sha` marker | `mobile` | #6 (`6a3c8aa`, `ebd9dc6`) | **Yes** — `curl https://portofino-essen.com/` carries `build-sha=35bf7e0d…`, which is `origin/main`. |\n> | 1 backend — `commit` on `/api/health` | `backend` | #1 (`9df5b85`) | **No** — the live `/api/health` answers `{\"status\":\"ok\",\"stripe\":\"mock\"}`, no `commit`; the image carrying it has not been deployed (Phase 4b). |\n> | 2 — SSM channel, OIDC, two roles | `infra` | #1 (`f828054`), fixed by `3e02a93` | **Applied** — the mobile deploy assumes `portofino-ci-web` and reads all three parameters; `AWS_DEPLOY_ROLE_ARN` was set 2026-09-11 08:09. |\n> | 3 — web deploy workflow | `mobile` | #6; follow-up in the PR that carries this status | **Yes** — first green run was the 08:12 `workflow_dispatch` (first publish of `ebd9dc6`); the first push-triggered green deploy carried #8 (`35bf7e0`, run 34599882565). |\n> | 4a — backend CI on Postgres | `backend` | #1, #2 | CI runs. |\n> | 4b — backend deploy through `production-backend` | `backend` | #1, #2 | **No** — every push run so far failed at its own guard, *\"Environment 'production-backend' has no required reviewer\"*. The environment now carries `required_reviewers`; no run has been made since, so the next push to `master` (or a dispatch) is the one that will go through the gate. |\n> | 5 — retire the Terraform deploy path | `infra` | #1 (`f828054`); README rewritten | Both `null_resource`s are gone from `master`. Apply-then-plan cleanliness: not read from here. |\n>\n> **Of the plan's own five verification items, one is done.** (1) the live\n> marker moved with a merge — done, twice. (2) the backend marker — blocked on\n> 4b. (3) a mid-flight deploy observed from a warm-cache browser — not done,\n> and cannot be done from CI. (4) **neither rollback has been executed** — the\n> one web dispatch so far published `main`'s head, which is a first publish,\n> not a rollback; the path is `gh workflow run deploy-web.yml -f ref=<sha>` and\n> it remains a claim until it is run once on purpose. (5) apply-then-plan —\n> not read. The lifecycle rule Phase 3 named turned out to be the wrong tool\n> and infra deliberately ships none (`infra/README.md`, \"The publish\n> invariant, and old builds\"); a reference-aware prune in the mobile workflow\n> is the replacement, and it is not built.\n>\n> Earlier: VETTED 2026-09-10.\n>>>>>>> 077b300 (fix(deploy): close the alarm on recovery, repeat the asset gate, document the deploy path)\n"
      ]
    }
  ]
}