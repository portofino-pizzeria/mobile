{
  "schema_version": 1,
  "base_sha": "35bf7e0de268e485b81aef5a3fa1068c385a6754",
  "ours_sha": "611eee05ef6e3fb2123eeddb89f5f369b3806a66",
  "theirs_sha": "e58e195416b27ec654b68d21fe90a310145e5825",
  "entries": [
    {
      "path": "src/app/index.tsx",
      "base_oid": "798e037bcd2f6e112ef1db3fb7b996ed3361582d",
      "ours_oid": "cffc62f32413d2e196ec1d688bb2b3cd243626fb",
      "theirs_oid": "855cad6f3b93ebc0c547bcaa09f03cea6bfb58b1",
      "base_path": "src/app/index.tsx.base",
      "ours_path": "src/app/index.tsx.ours",
      "theirs_path": "src/app/index.tsx.theirs",
      "hunks": [
        "<<<<<<< HEAD\n                    {item.description ? (\n                      <ThemedText type=\"small\" themeColor=\"textSecondary\">\n                        {item.description}\n                      </ThemedText>\n                    ) : null}\n                    <ThemedText type=\"small\" themeColor=\"textSecondary\">\n                      {allergenText(item, legendByCode)}\n                    </ThemedText>\n\n                    {item.variants.length === 0 ? (\n                      // The API returns an item with no priced variant rather than\n                      // hiding it. It is shown, and it is not orderable.\n                      <ThemedText type=\"small\" themeColor=\"textSecondary\">\n                        Zurzeit nicht bestellbar\n                      </ThemedText>\n                    ) : (\n                      <View style={styles.variants}>\n                        {item.variants.map((variant) => (\n                          <BridgeButton\n                            key={variant.id}\n                            uiId={addButtonUiId(item.id, variant.id)}\n                            uiLabel={`${item.name} (${variant.label}) in den Warenkorb legen`}\n                            style={[styles.addBtn, { borderColor: theme.brand }]}\n                            onPress={() => cart.add(item, variant)}>\n                            <ThemedText type=\"price\">\n                              + {variant.label} · {formatEUR(variant.price)}\n                            </ThemedText>\n                          </BridgeButton>\n                        ))}\n                      </View>\n                    )}\n                  </View>\n                </ThemedView>\n              );\n            })}\n=======\n                  ) : (\n                    <View style={styles.variants}>\n                      {item.variants.map((variant) => (\n                        <BridgeButton\n                          key={variant.id}\n                          uiId={addButtonUiId(item.id, variant.id)}\n                          uiLabel={`${item.name} (${variant.label}) in den Warenkorb legen`}\n                          // Pressed: the OUTLINE darkens to the declared pressed\n                          // red and the ground lifts to the app's selected\n                          // ground; the label keeps its `priceText` colour (see\n                          // the `brandPressed` token for why the label does not\n                          // move). The ground lift is what carries the press in\n                          // dark mode — a darker red on a dark card measures\n                          // ~2.4:1 and reads as fading, not pressing.\n                          style={({ pressed }) => [\n                            styles.addBtn,\n                            pressed\n                              ? { borderColor: theme.brandPressed, backgroundColor: theme.backgroundSelected }\n                              : { borderColor: theme.brand },\n                          ]}\n                          onPress={() => cart.add(item, variant)}>\n                          <ThemedText type=\"price\">\n                            + {variant.label} · {formatEUR(variant.price)}\n                          </ThemedText>\n                        </BridgeButton>\n                      ))}\n                    </View>\n                  )}\n                </View>\n              </ThemedView>\n            ))}\n>>>>>>> 611eee0 (refactor(theme): wire the pressed red, align tokens with visual-system v2, drop the template's dead surface)\n"
      ]
    }
  ]
}