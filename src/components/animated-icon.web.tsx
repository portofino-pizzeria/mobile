/**
 * Web has no native splash to hand off from, so there is nothing to overlay.
 * The native file animates the splash colour out; this one renders nothing.
 */
export function AnimatedSplashOverlay() {
  return null;
}
